SUBDIRECTORY CONTENTS

\GetHTTP2	Console utility that retrieves a file from a HTTP server
		and uses event objects and WSASelectEvent() for asynchronous
		notification.

\GetHTTP3	Console utility that retrieves a file from a HTTP server
		and uses WinSock 2 overlapped I/O functions.


